import { Router } from 'express';

const router = Router();

/*
 TODO:
 @GET
 Input: Artist Name
 Output: ArtistSearch object: { Artist image, Map of words in all songs by this artist -> Count of word }
*/

export default router;
