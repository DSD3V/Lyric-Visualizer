export const PASSWORD_MIN_LENGTH = 'Password must be at least 6 characters.';
export const PASSWORDS_MUST_MATCH = 'Passwords must match';
export const REQUIRED_ERROR_MESSAGE = 'This field is required.';
export const VALID_EMAIL_FORM = 'Email must be of the form _@_._';
export const VALID_EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
